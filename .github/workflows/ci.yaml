name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, labeled]
  merge_group:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        run: make build

  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Verify
        run: make verify

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Test
        run: make test

  test-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Integration Test
        run: make test-integration

  test-e2e:
    if: github.event_name == 'push' || github.event_name == 'merge_group' || contains(github.event.pull_request.labels.*.name, 'ok-to-test')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - uses: helm/kind-action@v1
        with:
          cluster_name: kind

      - name: Build images
        run: make image VERSION=e2e

      - name: Load images into kind
        run: |
          kind load docker-image ghcr.io/shaharyar-graph8/mbm/axon-controller:e2e
          kind load docker-image ghcr.io/shaharyar-graph8/mbm/axon-spawner:e2e
          kind load docker-image ghcr.io/shaharyar-graph8/mbm/claude-code:e2e
          kind load docker-image ghcr.io/shaharyar-graph8/mbm/codex:e2e

      - name: Build CLI
        run: make build WHAT=cmd/mbm

      - name: Deploy controller
        run: |
          bin/mbm install
          kubectl set image deployment/axon-controller-manager manager=ghcr.io/shaharyar-graph8/mbm/axon-controller:e2e -n axon-system
          kubectl patch deployment axon-controller-manager -n axon-system -p '{"spec":{"template":{"spec":{"containers":[{"name":"manager","imagePullPolicy":"Never","args":["--leader-elect","--spawner-image=ghcr.io/shaharyar-graph8/mbm/axon-spawner:e2e","--spawner-image-pull-policy=Never","--claude-code-image=ghcr.io/shaharyar-graph8/mbm/claude-code:e2e","--claude-code-image-pull-policy=Never","--codex-image=ghcr.io/shaharyar-graph8/mbm/codex:e2e","--codex-image-pull-policy=Never"]}]}}}}'
          kubectl wait --for=condition=available deployment/axon-controller-manager -n axon-system --timeout=120s

      - name: E2E Test
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          GITHUB_TOKEN: ${{ github.token }}
          AXON_BIN: ${{ github.workspace }}/bin/mbm
        run: make test-e2e
